
  # Component library design

  This is a code bundle for Component library design. The original project is available at https://www.figma.com/design/JZwgsWsEWkq2JNSoGbkkvF/Component-library-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  